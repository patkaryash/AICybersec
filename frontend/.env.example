# Sentinel AI Frontend Environment Configuration
# API base URL for the FastAPI backend (/api/v1 prefix is handled by apiClient)
VITE_API_BASE_URL=http://localhost:8000

# Set to "true" to run in isolated offline demo mode with mock data
# Set to "false" to use real backend API (/api/v1)
VITE_DEMO_MODE=false
